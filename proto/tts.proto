syntax = "proto3";


package tts_audio;


service ProtoAudioStream {
    rpc StreamAudio (ProtoPost) returns (stream ProtoResp) {}
}

message ProtoPost {
    string model = 1;
    string text = 2;
    string voice = 3;
    float speed = 4;
}

message ProtoResp {
    bytes data = 1;
}
